# E8.0: Prepare and submit the 60% supervisor draft

**Status:** active
**Assignee:** me
**Planned:** null

## Goal

A PDF covering Introduction through Results (plus commenced Discussion) reaches the
supervisor, formatted to MAU rules.

## Context

This is the course's 60% checkpoint, not the final submission. Per the assignment
instructions, the draft must include: Introduction, Literature Review, Methodology,
Results, and ideally commenced Analysis/Discussion, plus References/Appendices,
formatted per MAU's guidelines and using the intended final template. It's graded
Complete/Incomplete only — the substantive feedback comes from the supervisor directly,
not through this milestone.

**Content-wise, the thesis already clears the bar for this checkpoint and then some:**
§1 (Introduction), §2 (Background/Literature Review), §3 (Methodology), §4 (Data), §5-§6
(Results for RQ1/RQ2), and §7 (the RQ3 placement artefact) are all fully drafted — that's
further than "up to and including Results" asks for. §8 Discussion is not started; a
commenced version would exceed the requirement but isn't mandatory ("if possible").

**What's actually missing is mechanical, not intellectual — three mandatory front-matter
sections are empty stubs, and the PDF has never been fully assembled with a working
bibliography:**

- Abstract, Popular Science Summary, and Declaration of AI Usage are all mandatory per
  `writing/thesis-structure.md` and are currently just `% TODO` comments (see E8.1-T3,
  T4, T5).
- The MAU logo banner (`writing/images/logo.jpg`) has never been added, so the
  mandatory front page graphic falls back to a draft placeholder (E8.2-T6).
- `\bibliographystyle{IEEEtran}` requires `IEEEtran.bst`, which is not present in the
  build toolchain, and `bibtex` has never actually been run — the reference list has
  never rendered in any compiled PDF to date. Verified 2026-07-01: switching to the
  bundled `ieeetr` style produces a clean IEEE-numeric list from the existing
  `references.bib` with zero errors (E8.2-T8).

## Tasks

- **E8.0-T1** — Confirm the four items above (E8.1-T3, T4, T5, E8.2-T6, E8.2-T8) are
  done before attempting the final compile for this checkpoint.
- **E8.0-T2** — Full clean compile: `pdflatex` x2, `bibtex`, `pdflatex` again. Zero
  errors, a rendered numbered reference list, and no missing-file warnings.
- **E8.0-T3** — Visual proof pass against `writing/thesis-structure.md`: 30 mm margins
  (already set), Latin Modern 11 pt body (already set, not Times), single spacing
  (already set), justified text (already set), numbered headings, banner present,
  page numbers on main content.
- **E8.0-T4** — Submit the resulting PDF to the supervisor / course system (human
  action — Canvas or direct email per the assignment instructions).

## Exit

Supervisor has the PDF and confirms receipt; the course's 60% milestone can be marked
Complete.
