# {{ID}}: {{TITLE}}

> Long-form body for this epic. The board (`roadmap.json`) holds the one-line
> `goal` and `exit`; this file holds the reasoning, context, and detail an agent
> needs to actually do the work. Keep the two in sync: the `goal`/`exit` here
> expand on the same `goal`/`exit` in the JSON, they never contradict it.

**Status:** {{STATUS}}
**Assignee:** {{ASSIGNEE}}
**Planned:** {{PLANNED}}

## Goal

{{GOAL}}

## Context

Why this epic exists, what it depends on, what it explicitly does NOT cover.
Link the relevant specs and contracts here so the agent reads them first.

## Tasks

Mirror of the tasks in `roadmap.json`, with detail. The JSON is the source of
truth for status; this is the source of truth for *how*.

- {{ID}}-T1: ...

## Exit

{{EXIT}}
